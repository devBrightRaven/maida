---
tags:
  - maida
  - user-manual
  - chinese
status: published
source: claude-code
project: maida
created: 2025-01-01
updated: 2026-04-20
description: "Maida v0.4.3 使用手册（简体中文）：涵盖临（Rin）快速决策、构（Kamae）策展管理、型（Kata）分组、引导导览、锚定、冻结状态、键盘与手柄操作、设置。"
---

# Maida 使用手册

**版本：v0.4.3**

---

## 这是什么？

Maida 从你的 Steam 游戏库中，每次只显示**一款游戏**。

你来决定：现在玩，或者看看别的。

Maida 有两个面：
- **临（Rin）**：快速决策。骰子转出一款游戏，你选择试试或现在不要。
- **构（Kamae）**：策展管理。整理你的展示柜，决定哪些游戏进入骰子池。

---

## 安装前须知

### Windows：SmartScreen 警告

第一次运行 Maida 时，Windows 会显示**「Windows 已保护你的电脑」**。这是因为 Maida 没有经过代码签署 — 付费证书每年要 300 美元以上，对一人开发的免费软件并不合理。

继续安装：点击 **其他信息** → **仍要运行**。Windows 累积足够下载量后就不会再显示这个警告（SmartScreen 信誉会自动建立，你不需要做任何事）。

如果你想在信任前先验证：
- 源代码公开：[github.com/devBrightRaven/maida](https://github.com/devBrightRaven/maida)（MIT 授权，每一行都可以读）
- 默认不收集任何数据；选择加入后只会送出匿名的版本 ping
- 开发者可联系：bertram@brightraven.world

### ROG Ally：手柄模式

若使用 ROG Ally 而按键在 Maida 中没有反应，请将 **Armoury Crate** 设为 **手柄模式（gamepad mode）**，或将 Maida 加入游戏 profile。这样按键才会以 gamepad 信号送入，而非键盘鼠标映射。

---

## 首次设置

1. 确保 **Steam 正在运行**
2. 打开 Maida
3. 点击「扫描 Steam 游戏库」
4. 等待扫描完成（数据不会离开你的设备）
5. 完成 — Maida 会用 3 款随机游戏创建「示范型」让你开始

Maida 会自动检测你的系统语言（英文、日文、简体中文、繁体中文）。你可以之后在设置中更改。

---

## 引导导览

第一次进入临（Rin）时，注意右上角的 **?** 按钮。点击它（或按 **F1**）可以启动引导导览，带你了解：

- 游戏标题与处方签的意思
- 「试试」与「现在不要」的运作方式
- 如何切换到构模式
- 如何在构中管理型与游戏
- 如何切换回临

导览随时可用——需要复习时，按 ? 或 F1 即可。

---

## 临（Rin）：快速决策

你会看到：
- 游戏标题
- 一句反思（称为「处方签」）
- 两个按钮：**「试试」** 和 **「现在不要」**

### 试试

如果你现在愿意玩这款游戏，**点击试试**。

会发生什么：
- 游戏立即启动
- Maida 记住你选择了这款游戏（权重 +1）
- 画面进入冻结状态

### 现在不要

如果你现在不想玩这款游戏，**点击现在不要**。

会发生什么：
- 显示下一款游戏
- 这款游戏在本次 session 中不会再出现
- 重复拒绝的游戏会越来越少出现

---

## 构（Kamae）：策展管理

从临的画面点击右侧边缘的切换按钮，或按 **Ctrl+Tab** / **RB** 进入构。

在构里你可以：
- 浏览你展示柜里的游戏
- 搜索已安装的游戏并加入展示柜
- 建立和管理「型」（Kata）

### 型（Kata）

型是展示柜的子分组。你可以依照心情或主题建立不同的型。

- 最多建立 2 个型（每个最多 15 款游戏）
- 在临面中，骰子只会从启用中的型里挑选
- 选择「全部游戏」则从整个展示柜挑选
- 按 F2 或手柄 Y 键重命名型

#### 操作方式

| 操作 | 方法 |
|------|------|
| 建立型 | 按「新增型」按钮 |
| 删除型 | 点击「x」按钮（需确认两次） |
| 重命名 | 按 F2 或手柄 Y 键 |
| 加入游戏 | 点击「+」后用勾选框选游戏，或用下方搜索框按名称搜索 |
| 移除游戏 | 按「移除」，按钮变成「确定？」后再按一次确认。或长按 Enter/A 2.5 秒。 |

---

## 这样就够了

使用 Maida 不需要知道更多。以下都是选读。

---

## 选读：锚定（把游戏钉在画面上）

有时候你想先决定，但稍后再玩。

**长按「试试」按钮 3 秒。**

会发生什么：
- 游戏被「锚定」在画面上
- 「试试」按钮变成「开打」
- 「现在不要」按钮变成「清除」
- 下次打开 Maida 时，这款游戏仍然在这里

**改主意了？** 点击「清除」即可。

---

## 选读：撤销

做错了？

点击画面底部的「撤销（仅限一次）」按钮。

回到上一步。但每次决定只能用一次。

注意：撤销会让游戏重新出现在画面上，但 -2 的权重扣分不会恢复。分数会在约一周内通过每日衰减自然恢复。

---

## 冻结状态

### 什么是冻结？

点击「试试」或「开打」启动游戏后，Maida 进入冻结状态。

冻结的意思是：
- 你已经做出承诺
- 系统停止显示新选项
- 保护你的决定不被其他选择干扰

**这不是暂停，是保护你的专注。**

### 如何解除冻结？

游戏结束后，回到 Maida，点击「再来一次」。

注意：按钮会在休息时间后才启用（默认 15 秒，可在设置 → 无障碍 → 休息秒数调整为 5 至 30 秒），防止意外连按。

---

## Maida 如何挑选游戏

Maida 观察你的选择：
- 你选择「试试」的游戏 → 更常出现
- 你选择「现在不要」的游戏 → 更少出现
- 所有权重每天自动衰减（趋向中性）

它不是在「推荐」。它只是诚实地反映你的选择倾向。

---

## 键盘与手柄操作

### 导览（所有模式）

| 操作 | 键盘 | 手柄 |
|------|------|------|
| 移动焦点 | 方向键或 Tab | D-pad 或左摇杆 |
| 滚动长页面 | Page Up/Down | 右摇杆，或在法律页面可用 D-pad 上下 |

### 临模式

| 操作 | 键盘 | 手柄 |
|------|------|------|
| 试试 | Enter | A |
| 现在不要 | Enter | A |
| 锚定 | 长按 Enter 3 秒 | 长按 A 3 秒 |
| 撤销 | Enter | A |
| 切换到构 | Ctrl+Tab | RB |

### 构模式

| 操作 | 键盘 | 手柄 |
|------|------|------|
| 选择型 | Enter | A |
| 重命名型 | F2 | Y |
| 删除型 | x 按钮（按两次） | A on x（按两次） |
| 移除游戏 | 长按 Enter 2.5 秒，或按两次 | 长按 A 2.5 秒，或按两次 |
| 切换到临 | Ctrl+Tab | LB |
| 设置 | Enter | A |

### 通用

| 操作 | 键盘 | 手柄 |
|------|------|------|
| 切换浅色/深色 | 点击右上角按钮 | D-pad + A |
| 帮助／导览 | F1 或 ? 按钮 | — |
| 返回 | Escape | B |

支持 NVDA 屏幕阅读器（Windows）。Linux 上由于 WebKitGTK 限制，Orca 支持有限。

---

## 设置

在构面中点击「设置」。每个区块都可以点击标题展开。

- **语言**：选择英文、日文、简体中文或繁体中文。选择语言后再按一次确认。
- **无障碍**：展开区块含三项偏好设置：
  - **振动回馈**：关闭 / 低 / 中 / 高。变更立即生效。（仅手柄）
  - **休息秒数**：试试后「再来一次」按钮启用前的休息时间。5 到 30 秒的滑杆（默认 15 秒）。数值会跨启动持续保留。
  - **查看完整无障碍声明**：打开无障碍政策页面。
- **操控说明**：完整的键盘与手柄快捷键列表。
- **主题**：切换浅色/深色模式。
- **IGDB 游戏时长数据**：选填。输入 Twitch Developer 凭证后，游戏标题下方会显示预估游玩时长。
- **匿名使用数据**：每次启动时发送一个小型匿名信号（随机 ID、安装天数、版本号）。可随时关闭。

---

## 常见问题

**Q：为什么一直出现同一款游戏？**
你之前多次选择了「试试」。Maida 不追踪游玩时间，只记录你的选择。如果不想再看到，选「现在不要」。

**Q：可以搜索特定游戏吗？**
在构面中可以搜索并加入展示柜。在临面中不行，那是骰子的工作。

**Q：追踪游玩时间吗？**
不会。只知道你选了「试试」还是「现在不要」。

**Q：数据会上传吗？**
不会。所有数据存在你的设备上。

---

## 疑难排解

### 「检测不到 Steam」

Maida 在磁盘上找不到你的 Steam 安装目录。它会在标准位置寻找 `steamapps/libraryfolders.vdf` 这个文件：

- Windows：`C:\Program Files (x86)\Steam`、`C:\Steam`、`D:\Steam`、`E:\Steam`
- Linux：`~/.steam/steam`、`~/.local/share/Steam`、以及 Flatpak 路径
- （Windows 还会检查 registry 中的自定义安装路径）

常见原因：

- Steam 没安装 → 从 store.steampowered.com 安装
- Steam 装在非标准位置 → 在启动 Maida 前设置环境变量 `MAIDA_WINDOWS_STEAM_ROOT` 指向你的 Steam 文件夹

Steam 不需要在运行中。Maida 直接从磁盘读取你的游戏库，启动游戏时通过 Steam URL（会在需要时自动唤醒 Steam）。

### 按钮没反应

大多数按钮（TRY、Not Now、PLAY、Clear、I'm Back）只需要快速点击。锚定（Anchor）需要在 TRY 上长按 3 秒。这是刻意设计，防止手抖或手柄碰撞造成的误启动。

如果按钮没反应，你大概卡在中间区段：太久不算 tap，又没到 3 秒不算 hold。试着更快地 tap，或确实长按完整 3 秒。

### Windows 说「这个应用程序可能不安全」
这是 SmartScreen 警告。完整说明与处理方式请见上方的 **安装前须知** 段落。

### 游戏没有启动
确认 Steam 能正常启动该游戏。某些游戏需要首次手动设置。

---

## 设计理念（有兴趣的话）

大多数游戏启动器帮你浏览。浏览你的游戏库会产生决策疲劳。

Maida 完全移除浏览。一款游戏。一个选择。继续前进。

> maida is the space you pass through before you begin.

---

联系：bertram@brightraven.world

App Version: v0.4.3 | Manual Version: v0.4.3
最后更新：2026-04-26
